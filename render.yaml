services:
  - type: web
    name: Demon-uploader 
    plan: free
    env: docker
    dockerfilePath: Dockerfile
    repo: https://github.com/DemonArmyBot/Demon-uploader
    branch: main
    autoDeploy: false
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: BOT_NAME
        sync: false
      - key: API_ID
        sync: false
      - key: API_HASH
        sync: false
      #- key: WEBHOOK
        #sync: false
      - key: PORT
        value: 8000  # Default port value, update if needed
